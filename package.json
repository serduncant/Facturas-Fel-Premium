{
  "name": "visualizador-facturas-fel-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:both": "npm run dev --prefix frontend & npm run dev --prefix backend"
  }
}
